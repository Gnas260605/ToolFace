# Subscription Reconciliation

The current repository includes a service-level reconciliation path through webhook processing and lazy trial provisioning.

Future work should move this into a scheduled worker job once the broader background-job foundations are completed for billing.
